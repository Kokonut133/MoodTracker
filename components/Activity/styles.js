import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex:1,
    backgroundColor: '#00008b',
  },
  belowButton: {
    color: 'white',
    fontSize: 12,
  }
});